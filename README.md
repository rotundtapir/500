Screenshots attached to issues in this repository.

This is an orphan branch that intentionally has no history in common with `main`;
it exists only so issue bodies can link images by raw URL. Do not merge it.
