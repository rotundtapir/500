# SPDX-License-Identifier: GPL-3.0-or-later WITH LicenseRef-cardkit-ads-exception
{
	email {$ACME_EMAIL}
	# First bring-up only: uncomment to use Let's Encrypt staging while DNS/config is being sorted,
	# so a fumble can't burn the production certificate rate limit. Comment out and
	# `docker compose restart caddy` once a staging cert issues end to end.
	# acme_ca https://acme-staging-v02.api.letsencrypt.org/directory
}

# One vhost file per game, each dropped in by that game's own deploy job. 500's lives in
# sites/500.caddy in this repo; a second game (euchre) adds its own without touching this file or
# 500's, which is what lets the two deploy independently through one Caddy.
import /etc/caddy/sites/*.caddy
